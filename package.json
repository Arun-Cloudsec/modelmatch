{
  "name": "modelmatch-post",
  "version": "1.1.0",
  "description": "ModelMatch AI \u2014 LinkedIn launch page (catalog refreshed 2026-08-21)",
  "scripts": {
    "start": "serve . -l $PORT --no-clipboard",
    "test": "node catalog-test.js"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
