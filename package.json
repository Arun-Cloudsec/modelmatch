{
  "name": "modelmatch-post",
  "version": "1.0.0",
  "description": "ModelMatch AI — LinkedIn launch page",
  "scripts": {
    "start": "serve . -l $PORT --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
